Instalacion:
1) copiar contenido en una carpeta en el servidor
2) Ejecutar la consulta la consulta del fichero basedatosejemplo.sql para instalar la base de datos

ANOTACIONES:

El puerto de la conexion a la base de datos es la 3307, no es la por defecto que es 3306. Es porque estoy usando MariDb
Si necesita cambiar el puerto puede hacerlo en el fichero classDatabase.php que se encuentra en la carpeta database
